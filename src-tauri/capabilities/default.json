{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Grove reads registered repositories, lists projects and changes, reads one file diff, opens paths inside a registered project, picks a directory, restores the window, and listens for the watcher event.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open",
    "store:default",
    "window-state:default"
  ]
}
