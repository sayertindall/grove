# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# The vendored component library is a delivery vehicle for the port under
# src/components/beautiful, not repository content: it is a full Next.js app with
# ~980 MB of node_modules and build output. The port carries the upstream URL and
# the MIT notice. Safe to delete the directory once the port is verified.
beautiful-ui/

# Local coding-agent configuration for this checkout; not repository content.
.omp/

